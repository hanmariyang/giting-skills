# examples
`example-reference.jpg` is a synthetic texture reference (not a real product) used to
demo the color extractor; `example-palette.png` is its extracted swatch. Real use pulls
color from your actual product reference.
